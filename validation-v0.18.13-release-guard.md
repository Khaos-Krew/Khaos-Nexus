# Guarded release validation

Confirm the same-repository `publish/vX.Y.Z` approval path checks out only the validated release base SHA, rejects mismatched branches or forked trigger repositories, runs complete tests and checks, verifies every updater asset, and publishes the exact validated release target.

# Publish Khaos Nexus v0.22.1

Owner-requested stable publication approval for the local desktop module recovery hotfix.

The stable workflow must build and publish the base commit from `release/v0.22.1` after running the complete tests, syntax checks, Windows installer and portable packaging, updater asset verification, and checksum generation.

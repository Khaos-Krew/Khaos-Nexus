# v0.18.11 validation

Validate that the main application grid establishes bounded scroll containers, the left navigation and right workspace scroll independently in software and rich-brand modes, nested log and diagnostic outputs retain scrolling, the successful v0.18.10 sandbox preload/startup release path remains unchanged, portable sidecar diagnostics still initialize before the single-instance lock, and both Windows installer and portable executable package successfully.

# v0.18.4 validation

Validate that the dedicated startup splash is excluded from main-interface stability injection and heartbeat monitoring, the main renderer remains protected, the verified base interface releases after the thirty-second minimum even when optional feature completion is delayed, v0.17-compatible profile recovery remains intact, and Windows installer/portable packaging succeeds.

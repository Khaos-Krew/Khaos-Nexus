# v0.18.1 validation

Validate prior configuration discovery, saved Discord session restoration before access enforcement, CSP-safe preload splash creation, real module progress, timeout recovery, and Windows packaging.

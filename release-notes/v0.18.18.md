# Khaos Nexus v0.18.18

This maintenance checkpoint preserves the owner-verified v0.18.17 startup and renderer-unfreeze behavior while tightening Discord runtime state synchronization.

## Fixed

- Prevents the Command Center task rail from allowing an older startup snapshot to overwrite a newer live Discord state.
- Treats a ready Discord identity as online when a compact card receives a transient starting or connecting label.
- Reports the supervised Discord utility-process PID after Electron confirms the child process has spawned.
- Keeps stopping, crash, restart, heartbeat, updater, backup, startup gate, navigation, and local configuration behavior unchanged.

## Validation

- Targeted runtime-state regression tests.
- JavaScript syntax checks for modified main and renderer files.
- Full repository CI, package checks, and Windows packaging are required before owner testing.

# Khaos Nexus v0.18.21

## Grouped navigation click routing

- Fixes the grouped navigation buttons rendering correctly but not opening their workspaces.
- Corrects the proxy target lookup to read `data-view-proxy` through `dataset.viewProxy` instead of the unrelated `dataset.view` property.
- Keeps the original working navigation buttons in place and safely forwards each grouped proxy click to its matching original button.
- Adds missing-target and routing-warning diagnostics so any future navigation mismatch is captured by the existing monitor instead of failing silently.
- Retains the searchable collapsible navigation groups and the always-visible in-app Update Center from v0.18.20.
- Retains the Palworld status-panel Discord emoji correction, including the valid `🔄` Refresh button payload.
- Retains mandatory verified pre-update backups, automatic error capture, the five-minute startup error batch, thirty-minute maintenance scans, protected credentials, local configuration, startup validation, and portable diagnostics.

This is a guarded Windows production candidate. Stable publication remains blocked until workspace navigation, the in-app updater, Palworld status-panel publishing, startup, Discord runtime, and the error monitor are confirmed on the owner PC.
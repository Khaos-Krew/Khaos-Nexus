Approve publication of the validated release/v0.18.13 base commit through the guarded stable GitHub release workflow.

The default-branch workflow now supports this same-repository approval path and checks out only the validated release base SHA.

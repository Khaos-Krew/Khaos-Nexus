; KHAOS NEXUS ARK CANONICAL GAMEUSERSETTINGS.INI
; Source of truth: Khaos-Krew/Khaos-Nexus
; Bootstrap is intentionally incomplete. Do not deploy until manifest.json has
; bootstrap_complete=true and deployment_enabled=true.
; Never commit ServerAdminPassword, ServerPassword, RCONPassword, tokens, or webhooks.

[ServerSettings]
; Canonical shared non-secret server settings will be populated here from the
; reviewed known-good Astraeos export. After that import, Git is authoritative.

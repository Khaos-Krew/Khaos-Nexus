name: Updater Beta Release (superseded)

on:
  push:
    branches:
      - "updater/v0.26.5-beta"

concurrency:
  group: updater-beta-v0.26.5
  cancel-in-progress: true

jobs:
  superseded:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publication is owned by the protected pull_request_target workflow on release/v0.26.5-beta."

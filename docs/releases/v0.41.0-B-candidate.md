# v0.41.0-B Release Candidate Promotion

Validated implementation source: `a87630cd5ff422fb42a80ffea1f00dea00031c98`

This marker promotes the validated Nexus Core v1 implementation into the protected v0.41.0-B release workflow. It changes no runtime, renderer, AI runtime, adapter, updater, or packaging implementation code.

The protected publisher must rebuild and pass its own dependency audit, full tests/checks, embedded AI verification, packaged-content audit, packaged-startup smoke, clean-install smoke, updater metadata verification, release-manifest verification, and Windows signature policy before it may create `v0.41.0-B` or publish the GitHub release.

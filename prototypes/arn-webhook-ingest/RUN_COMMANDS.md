# Commands

```bash
cd prototypes/arn-webhook-ingest
npm install
node --test
npm start
```

Start with `ARN_DRY_RUN=true` in the environment. Switch to `false` only after the parser output is confirmed in logs.

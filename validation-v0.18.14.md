# v0.18.14 validation

Validate that the simplified updater always exposes a visible primary control in Desktop Settings, uses the existing settings action row when `#nexusUpdateCenter` is absent, leaves the legacy check button visible until the replacement exists, creates a fallback update panel only when necessary, preserves Check → Download → Install & Restart, retains the mandatory verified pre-update backup, and preserves the owner-verified startup, scrolling, portable diagnostics, grouped navigation, server control, and Discord runtime behavior.

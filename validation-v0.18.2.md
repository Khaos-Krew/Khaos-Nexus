# v0.18.2 validation

Validate bridge-first preload startup, deferred splash DOM installation, configuration restoration, saved Discord restoration, non-locking access-recovery fallback, scheduled error batches, and Windows packaging.
